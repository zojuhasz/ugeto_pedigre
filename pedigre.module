<?php

/**
 * Implementing hook_theme().
 */
function pedigre_theme($existing, $type, $theme, $path) {
  return array(
    'pedigre' => array(
      'variables' => array('items' => array(), 'title' => '')
    )
  );
}
